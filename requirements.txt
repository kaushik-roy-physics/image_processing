# Core scientific stack
numpy>=1.24
scipy>=1.10
tifffile>=2023.7.10
PyYAML>=6.0
joblib>=1.3
matplotlib>=3.7

# Segmentation (Cellpose-SAM / v4). Install a CUDA-enabled torch separately
# for GPU acceleration (see README).
cellpose>=4.0
torch>=2.0

# Microscope file readers
nd2>=0.10                 # .nd2 (Nikon)

# Universal fallback reader for .oir, .czi, .lif, ... (install the plugin you need)
bioio>=3.0
# bioio-bioformats        # .oir and many others (requires a Java runtime)
# bioio-czi               # .czi (Zeiss)
# bioio-lif               # .lif (Leica)
# bioio-ome-tiff          # .ome.tif (optional; tifffile already handles these)
